version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel-allinone-green
    image: demoage
    ports:
      - "8089:80"
    volumes:
      - ./docker/nginx-log:/var/log/nginx
    networks:
      - laravel_net

networks:
  laravel_net:
    external: true
