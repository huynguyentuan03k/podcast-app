name: Test Actions

on:
    push:
        branches: [main]
    workflow_dispatch:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Echo test
              run: echo "GitHub Actions is working!"
