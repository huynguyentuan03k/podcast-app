stages:
  - deploy

deploy:
  stage: deploy
  image: alpine:3.20
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: on_success
    - when: never
  before_script:
    - apk add --no-cache git openssh-client
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - echo "$DOKKU_SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -p "${DOKKU_SSH_PORT:-22}" -H "$DOKKU_SSH_HOST" >> ~/.ssh/known_hosts
    - git config --global user.email "${GITLAB_USER_EMAIL:-ci@example.com}"
    - git config --global user.name "${GITLAB_USER_NAME:-GitLab CI}"
    - git remote add dokku "ssh://dokku@${DOKKU_SSH_HOST}:${DOKKU_SSH_PORT:-22}/${DOKKU_APP_NAME}"
  script:
    - git push --force dokku "HEAD:main"
