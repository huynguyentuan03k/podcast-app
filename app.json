{
  "scripts": {
    "dokku": {
      "predeploy": "php artisan config:clear && php artisan cache:clear && php artisan route:clear",
      "postdeploy": "php artisan config:cache && php artisan route:cache"
    }
  }
}
